<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Page not found — Areterra Hub</title>
    <style>
        * { box-sizing: border-box; }
        body { margin: 0; min-height: 100vh; display: grid; place-items: center; padding: 24px; background: #f5f8f6; color: #213444; font-family: Inter, ui-sans-serif, system-ui, sans-serif; }
        main { width: min(100%, 520px); padding: 40px; border-radius: 24px; background: white; box-shadow: 0 18px 50px rgba(0, 52, 92, .12); text-align: center; }
        .mark { width: 64px; height: 64px; display: grid; place-items: center; margin: 0 auto 20px; border-radius: 20px; background: #e8f7f4; font-size: 30px; }
        .eyebrow { color: #007b83; font-size: 12px; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
        h1 { margin: 8px 0 10px; color: #00345c; font-size: clamp(28px, 7vw, 40px); }
        p { margin: 0 auto 24px; color: #5f6f7a; line-height: 1.6; }
        a { display: inline-block; border-radius: 999px; background: #007b83; padding: 12px 22px; color: white; font-weight: 750; text-decoration: none; }
        a:focus-visible { outline: 3px solid #f4c542; outline-offset: 3px; }
    </style>
</head>
<body>
    <main>
        <div class="mark" aria-hidden="true">🐾</div>
        <div class="eyebrow">Error 404</div>
        <h1>We couldn't find that page</h1>
        <p>The link may be out of date, or the page may have moved. Return to the Hub and continue from there.</p>
        <a href="/">Return to the dashboard</a>
    </main>
</body>
</html>
